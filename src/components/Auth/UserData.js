import * as React from 'react';
import {View, Text} from 'react-native';

export default function UserData() {
    
      const auth = null;
    
      return (
     <View>
        <Text>User Data</Text>
     </View>
      );
    }